Centauri.fn.__FormInputFix = () => {
    var i = 0;

    $("input", $("#modal-new_contentelement")).each(function() {
        // let $input = $(this);

        // $input.attr("id", $input.attr("id") + "-" + i);
        // $input.next().attr("for", $input.next().attr("for") + "-" + i);
        
        // i++;
    });
};
