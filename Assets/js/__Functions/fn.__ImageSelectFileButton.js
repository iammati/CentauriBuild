Centauri.fn.__ImageSelectFileButton = () => {
    $("div[data-trigger='selectfile'], button[data-trigger='selectfile']").each(() => {
        $(this).on("click", this, () => {
            
        });
    });
};
